import { Uci } from './types';
import { Position } from './chess';
export declare function makeSanAndPlay(pos: Position, uci: Uci): string;
export declare function makeSanVariation(pos: Position, variation: Uci[]): string;
export declare function makeSan(pos: Position, uci: Uci): string;
